{{#if network}}
  {{#if username}}
    {{#is (lowercase network) "linkedin"}}
      {{> info-tag text=(concat 'in/' this.username) icon="fab fa-linkedin" }}
    {{else}}
      {{> info-tag text=this.username icon=(concat "fab fa-" (lowercase network)) }}
    {{/is}}
  {{/if}}
{{/if}}
